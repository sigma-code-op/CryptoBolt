{
  "name": "cryptobolt",
  "version": "1.0.0",
  "private": true,
  "description": "CryptoBolt frontend build tooling — Tailwind CSS compile, JS bundling/minification, and CSP <meta> tag generation for the static site. The backend proxy lives in server/ with its own package.json.",
  "type": "module",
  "scripts": {
    "build": "npm run build:css && npm run build:js && npm run build:csp",
    "build:css": "tailwindcss -i css/tailwind-input.css -o css/tailwind.css --minify",
    "build:js": "node scripts/build-js.js",
    "build:blog": "node scripts/build-blog.js",
    "build:csp": "node scripts/build-csp.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "terser": "^5.36.0"
  }
}